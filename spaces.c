#include<stdio.h>
#include<conio.h>
void main()
{
int i,count=0;
char name[50];
clrscr();
gets(name);
for(i=0;a[i]!='\0';i++)
{
if(a[i]==' ')
{
count++;
}
}
printf("number of spaces in your entered string is: %d",count);
getch();
}
